<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for syncing with the backend -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for WorkManager scheduling -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required for task notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for exact alarms (task reminders) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".JamrahApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_jamrah"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_jamrah"
        android:supportsRtl="false"
        android:theme="@style/Theme.JamrahApp"
        android:usesCleartextTraffic="true">

        <!-- Disable default WorkManager initialization so Hilt can handle it -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>